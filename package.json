{
  "name": "playpen",
  "version": "0.1.0",
  "private": true,
  "description": "PlayPen — comedic-menace escape platformer. Fully data-driven; Player + Editor in one executable.",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "app": "npm run build && electron .",
    "app:dev": "electron . --dev",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=playpen --branch=master"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
